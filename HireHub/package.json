{
  "name": "hirehub-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "cd backend && npm install --omit=dev && npm run start",
    "heroku-postbuild": "cd backend && npm install --omit=dev"
  }
}
